P2P SPAWN FIX 2.0.1

P2P Spawn Fix protects Green Hell co-op clients from incomplete or invalid
network state. Confirmed results include restoring invisible remote players,
stopping repetitive inventory-related object-spawn errors, and keeping the
Pottery Table functional when invalid ghost updates are received. Version
2.0.1 also limits empty-state interception to the malformed object-spawn
message being repaired and preserves maps, quest items, notebook data, and
other progression-sensitive objects.

The protection is local to the computer where the mod is installed. It does
not alter the host's save or force other players to use the repaired state.

The mod works automatically after it is loaded.

COMMAND

p2pfix
p2pfix status

Shows how many null spawn-data arrays were repaired and how many malformed
spawn messages had to be discarded by the safety barrier. It also reports
skipped zero-byte initial states, preserved progression-sensitive states,
repaired or deferred player state, and blocked invalid Pottery Table updates.

CONFIRMED FIXES

- Restores invisible hosts or remote players on the protected client.
- Stops repetitive P2P spawn exceptions associated with broken inventory and
  backpack behavior.
- Blocks invalid PotteryTable SetupGhost indices before the current craft
  object can disappear or an ArgumentOutOfRangeException can be thrown.
- Prevents the generic zero-byte-state guard from intercepting unrelated
  replication or suppressing map and progression-object initialization.

INSTALLATION

Install the mod on every computer whose Player.log shows the exception. For
consistent co-op behavior, using it on the host and all connected players is
recommended.

P2P Spawn Fix is a permanent mod. GreenHellModLoader loads it by default and
does not allow it to be unloaded while the game is running. Disable or remove
it before starting Green Hell if you need to run without the fix.

The main repair is restricted to the time in which
P2PObjectSpawnMessage.Deserialize is running. It does not change byte arrays
belonging to other network-message types.

When Replicator.OnSpawnMessage receives a zero-byte payload, the game normally
calls ReplicationComponent.Deserialize as an initial-state read anyway. The mod
skips that impossible read only when the same object-spawn message required a
null-array repair. Unrelated initial replication states keep the game's normal
behavior. Maps, quest items, notebook/journal objects, recipes, and blueprints
are explicitly excluded from this generic skip.

If an empty initial spawn leaves ReplicatedPlayerSubelements without its
derived hash or current-state arrays, the mod rebuilds them from the remote
player model before OnReplicationResolve continues. If the incoming state or
the player model does not exist yet, that one resolution is deferred.

LOG BEHAVIOR

The mod logs the first three corrections, correction number 10, and then one
message for every 100 corrections. This prevents the original repetitive error
from flooding Player.log.
